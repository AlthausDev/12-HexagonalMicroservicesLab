spring:
  application:
    name: order-service

server:
  port: 8082

catalog:
  base-url: ${CATALOG_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
